<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 자동 텍스트 크기 조정. 갤럭시 폴드는 무조건 api 26 이상일 수 밖에 없어서 tartgetApi를 다음과 같이 정의, 하위 api level에서는 무시됨 -->
    <!-- 아래 style을 적용하려면 TextView에 maxLines를 꼭 지정해줘야 함 -->
    <style name="AutoSizeMaxFont_14">
        <item name="android:autoSizeTextType" tools:targetApi="o">uniform</item>
        <item name="android:autoSizeMinTextSize" tools:targetApi="o">4dp</item>
        <item name="android:autoSizeMaxTextSize" tools:targetApi="o">@dimen/font_size_small</item>
        <item name="android:autoSizePresetSizes" tools:targetApi="o">@dimen/font_size_small</item>
        <item name="android:autoSizeStepGranularity" tools:targetApi="o">1dp</item>
        <item name="android:ellipsize">end</item>
    </style>
</resources>