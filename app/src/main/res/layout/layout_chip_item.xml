<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingHorizontal="@dimen/layout_margin_xxxxsmall"
    android:paddingVertical="@dimen/layout_margin_xxxsmall"
    android:id="@+id/layout_root">
    <com.google.android.material.chip.Chip
        android:id="@+id/chip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:chipStrokeColor="@color/blackgray"
        app:chipStrokeWidth="1dp"
        app:chipBackgroundColor="@color/white"
        app:closeIcon="@drawable/ic_vector_close"
        app:ensureMinTouchTargetSize="false"
        app:closeIconStartPadding="0dp"
        app:closeIconEndPadding="0dp"
        android:checkable="false"
        android:clickable="true" />

</androidx.constraintlayout.widget.ConstraintLayout>