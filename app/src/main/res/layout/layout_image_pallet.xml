<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginVertical="@dimen/layout_margin_xxxsmall"
    android:layout_marginEnd="@dimen/layout_margin_xxxsmall"
    >
    <RelativeLayout
        android:id="@+id/layout_attach_image"
        android:layout_width="@dimen/image_pallet_size"
        android:layout_height="@dimen/image_pallet_size"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <ImageView
            android:id="@+id/iv_attach_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="1dp"
            android:scaleType="centerCrop" />

        <ImageView
            android:id="@+id/iv_action"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_alignParentRight="true"
            android:layout_marginStart="2dp"
            android:layout_marginTop="2dp"
            android:layout_marginEnd="2dp"
            android:layout_marginBottom="2dp"
            android:elevation="@dimen/layout_margin_small"
            android:paddingStart="@dimen/layout_margin_xsmall"
            android:paddingBottom="@dimen/layout_margin_xsmall"
            android:src="@drawable/ic_vector_close_white_shadow"
            android:stateListAnimator="@xml/animate_button_push" />

    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>